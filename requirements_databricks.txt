# Databricks integration dependencies

# Option 1: PySpark (if running on Databricks cluster or with Spark installed)
pyspark>=3.3.0

# Option 2: Databricks SQL Connector (lightweight alternative)
databricks-sql-connector>=2.0.0

# Data processing
pandas>=1.3.0
numpy>=1.20.0

# Existing dependencies (if not already installed)
pyyaml>=6.0
datacontract-cli>=0.9.0

# Optional: For enhanced logging and monitoring
python-dotenv>=0.19.0
